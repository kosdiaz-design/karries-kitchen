{
  "name": "karries-kitchen-v2",
  "private": true,
  "version": "2.0.0",
  "description": "v2 rebuild workspace. Legacy v1 lives at repo root in KarriesKitchen.html.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace server run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "migrate": "npm --workspace server run migrate",
    "start": "npm --workspace server run start",
    "typecheck": "npm --workspace client run typecheck && npm --workspace server run typecheck"
  }
}
